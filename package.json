{
  "name": "agent-ready-scaffold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/check-pin.mjs && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "bench": "node scripts/bench.mjs",
    "smoke": "node scripts/smoke.mjs",
    "oracle": "node scripts/diff-oracle.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
